# 极大层级与会话 DAG 性能验收

## 用户场景

- 32 个工作空间、249 个事项、1,992 个会话和 249 个画布从真实 SQLite 权威数据恢复。
- 单一画布包含 1,000 个同级会话，可持续横向滚动并切换到其它工作空间/事项。
- 5,000 层父子链和 10,000 节点宽 DAG 可重复布局；当前节点附近仍显示真实会话，远层按分支或层收拢，并直接显示会话总数、运行中、等待输入和异常数量。
- 用户点击远层聚合可逐层展开；缩放/平移让目标层进入附近范围时自动换回真实会话；搜索命中远层会话时直接定位到该真实会话。

## 可重复夹具

`tests/e2e/scale/scale-database.ts` 以固定时间和稳定 ID 写入真实迁移后的 Matou 数据库；重复写入前清除同一命名空间，数据库计数保持一致。运行：

```bash
pnpm test:scale
```

DAG 算法预算：

```bash
pnpm --filter @matou/desktop exec vitest run \
  src/renderer/src/dag/dag-layout.test.ts \
  src/renderer/src/dag/dag-render-model.test.ts \
  src/renderer/src/dag/DagCanvas.test.tsx \
  --disableConsoleIntercept
```

真实 Electron 10,000 节点全链路门禁：

```bash
pnpm test:dag-10000
```

该门禁从迁移后的真实 SQLite 数据库写入 10,000 个会话和 9,999 条结构关系，经 Runtime 与主窗口权威投影打开独立 DAG 窗口；不向 Renderer 注入图数据夹具。它同时执行标识符检查、共享包构建、全仓类型检查、相关 Runtime/Desktop 单元测试和生产构建。

## 指标

测量机器为 Apple M4 Pro / 14 CPU / 48 GiB。帧样本在真实 Electron 中持续推进横向滚动，而非只检查数据形状或空闲帧。

| 场景 | 基线 | 优化后 | 用户影响 |
| --- | ---: | ---: | --- |
| 1,000 前台终端归属 | 未区分 | 1,000 | 滑出视野不改变同级会话的前台身份；已经建立的 PTY 视图绑定和 xterm VT model 脱离卡片 DOM 继续保留，离屏增量由 Journal 衔接 |
| 1,000 会话卡片 DOM 节点 | 3,105 | 151 | 只虚拟化卡片展示及缓冲区，横向总长度、定位和终端生命周期保持不变 |
| 1,000 会话测量窗 SQL statements | 30,570 | 463 | 恢复/定时刷新不再按实体逐条读取 |
| Renderer RSS | 262.97 MiB | 235.20 MiB | 大画布内存下降 |
| Runtime RSS | 164.22 MiB | 89.44 MiB | 批量投影降低 Runtime 压力 |
| 帧 p50 / p95 | 6.9 / 7.2 ms（空闲采样） | 6.9 / 7.7 ms（持续滚动） | 更强滚动负载下仍保持稳定帧时间 |
| 32 工作空间 / 249 事项 / 1,992 会话恢复 | 未覆盖 | 445–516 ms | 启动后约半秒恢复到可操作主画布 |
| 跨工作空间事项切换 | 未覆盖 | 79 ms | 点击后快速显示目标的 4 会话画布 |
| 10,000 节点 DAG layout p50 / p95 | 未覆盖 | 5.21 / 5.39 ms | 打开关系视图的布局计算稳定 |
| 10,000 节点真实 Electron 首次可操作 | 3,567 ms | 254–258 ms（3 次） | 用户点击后在 300 ms 预算内看到可搜索、可平移和可缩放的真实关系图 |
| 聚合展开并定位 | 未覆盖 | 53.19–90.46 ms（3 次） | 点击远层聚合后，真实目标会话在 300 ms 预算内进入焦点 |
| 连续平移/缩放帧与输入响应 p95 | 未覆盖 | 8.90–9.10 / 8.70–9.00 ms（3 次） | 每次 180 帧连续手势保持流畅；测量窗未出现 Long Task |
| 远端会话搜索 | 未覆盖 | 11.09–11.51 ms（3 次） | 搜索 `dag 10000` 在 100 ms 预算内显示并可定位 |
| 5,000 层 DAG | 递归深度推导存在栈溢出风险 | 12 ms，迭代完成 | 深层关系可恢复并定位 |
| 深层 DAG 卡片 DOM | 远层 ghost 全量挂载 | 5,000 深链当前视口为 3 个真实会话 + 2 个聚合；10,000 会话 render model 的 node+aggregate `≤400`、edge `≤800` | 远层数量和关键状态仍可见，平移、缩放不随总节点数堆积 DOM |

## 稳定预算

- 1,000 会话持续滚动：前台终端绑定计数 `= 1,000`，卡片 DOM `< 700`，p95 `< 34 ms`，测量窗 SQL `< 1,500`。
- 完整层级目录恢复：`< 5,000 ms`；跨工作空间事项切换 `< 1,500 ms`。
- 10,000 节点布局 p95 `< 1,500 ms`；5 次预热/测量降低单次抖动影响。
- 真实 Electron 10,000 节点 DAG 从点击到首次可操作 `< 300 ms`；远端搜索 `< 100 ms`；聚合展开并定位 `< 300 ms`；连续平移/缩放帧 p95 `≤ 16.7 ms`，输入响应 p95 `< 34 ms`。
- 10,000 会话 DAG 的真实会话卡片与聚合卡片总和 `≤400`、关系边 `≤800`；聚合状态计数必须由成员会话计算，且总数不得丢失。
- 独立 DAG 窗口总 DOM 节点 `≤2,500`。
- Long Task p95 `< 100 ms`。

双屏验收同时读取 macOS 显示器清单和 Electron `screen` 映射：主屏必须为 `XV272U`；主窗口、DAG 窗口的可见边界均完整落在内部副屏（系统名称 `Color LCD`）工作区，主屏可见窗口数始终为零。

## 远层聚合专项回归

- `dag-render-model.test.ts` 使用真实的 10,000 会话深链和 1,200 会话混合分支数据，验证稳定聚合 key、总数守恒、运行中/等待输入/异常计数，以及 node+aggregate/edge 上限。
- `DagCanvas.test.tsx` 验证聚合卡片显示真实计数，点击聚合后目标会话进入附近层并显示为真实卡片，搜索命中远层会话时同样替换聚合并定位。
- 本节是 D-08 / Task 11 的聚合专项证据；发布结论仍以集成提交上的完整 Electron、scale 与打包门禁为准。

预算显著高于本机实测值，以容纳 CI/虚拟机差异，同时仍能拦截全量 DOM、逐实体查询和重复全图布局回归。
