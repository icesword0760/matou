# Matou 项目协作规则

## 用户沟通

- 所有面向用户的沟通，以产品、用户和具体使用场景的视角为主，优先说明问题、用户影响、适用场景、取舍和建议。
- 技术架构、代码、协议、数据结构和测试等内容仅作为结论的辅助证据，不作为沟通主体。
- 输出必须达到产品经理可据此做出决策的标准：结论清楚、影响明确、边界可判断；存在多个选择时，说明各自的用户价值、代价、风险并给出推荐。
- 功能交付和验收说明优先描述用户能看到什么、能完成什么、异常时会发生什么，再补充实现与验证证据。
- 已确认的规格进入实现后连续推进，不为常规技术选择、阶段计划或中间检查重复请求确认；仅当实现与 PRD 或 Kooky 既有产品行为产生实质冲突、需要产品取舍时再向用户确认。

## PRD 实现与验收基线

- 视觉与交互的 1:1 对照边界仅覆盖 Kooky 的 CLI 模块本身，即黑色工作区域：工作区/事项侧栏、标签栏、终端及分屏区域、快捷操作区，以及由这些入口触发的菜单、弹窗和状态反馈。
- Kooky 的顶部 Logo/品牌区域、最外侧产品导航栏及其它非 CLI 产品外壳不属于 Matou 的对照范围；Matou 保留自己的窗口标题与应用级导航设计。
- 每个 PRD 在提交产品验收前，必须以原始 Kooky 的可运行产品和现存代码逻辑为双重基线，完成该 PRD 范围内全部界面与交互的逐项比对。
- 比对范围覆盖：信息层级、布局、尺寸、间距、颜色、字体、图标、文案、默认状态、加载状态、空状态、悬停、焦点、选中、禁用、菜单、弹窗、键盘与鼠标操作、拖拽、窗口行为、异常反馈、恢复行为、持久化结果和操作后的状态迁移。
- Kooky 已存在的同范围能力，Matou 的 UI 样式、入口位置、操作步骤、反馈时机、交互逻辑和最终结果均按 1:1 一致验收；工程实现方式允许不同，用户可感知行为保持一致。
- 每个 PRD 的验收材料必须附带「Kooky 交互对照矩阵」，逐场景记录 Kooky 基线证据、Matou 实际结果、截图或运行证据及差异结论。矩阵未闭合时，该 PRD 继续处于实现阶段。
- 发现 Matou 与 Kooky 的差异时，优先对齐 Kooky；若 Kooky 行为与已确认 PRD 发生实质冲突，暂停对应冲突点并提交产品取舍，其余无冲突项继续推进。
